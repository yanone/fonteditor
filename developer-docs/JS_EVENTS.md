# JavaScript Event Reference

> Generated by `node generate-js-event-docs.mjs`. Do not edit this file by hand.

Generated: 2026-05-04T09:09:48.675Z

This document lists app-emitted JavaScript events discovered programmatically from source, along with generated descriptions, payload hints, emit sites, and listener sites.

## Summary

| Event | Class | Emitters | Listeners |
| --- | --- | ---: | ---: |
| [`canvasInitialReady`](#event-canvasinitialready) | `CustomEvent` | 1 | 1 |
| [`diskFilesChanged`](#event-diskfileschanged) | `CustomEvent` | 1 | 1 |
| [`diskFolderAccessChanged`](#event-diskfolderaccesschanged) | `CustomEvent` | 2 | 1 |
| [`editingFontCompiled`](#event-editingfontcompiled) | `CustomEvent` | 1 | 4 |
| [`editorModeChanged`](#event-editormodechanged) | `CustomEvent` | 3 | 7 |
| [`featureHistoryContextChanged`](#event-featurehistorycontextchanged) | `CustomEvent` | 1 | 1 |
| [`fileBrowserReady`](#event-filebrowserready) | `CustomEvent` | 1 | 0 |
| [`fontLoaded`](#event-fontloaded) | `CustomEvent` | 1 | 1 |
| [`fontModelReady`](#event-fontmodelready) | `CustomEvent` | 1 | 2 |
| [`fontModelSync`](#event-fontmodelsync) | `CustomEvent` | 1 | 1 |
| [`fontOpenLifecycle`](#event-fontopenlifecycle) | `CustomEvent` | 1 | 0 |
| [`fontReady`](#event-fontready) | `CustomEvent` | 2 | 8 |
| [`fontspectorUpdated`](#event-fontspectorupdated) | `CustomEvent` | 1 | 1 |
| [`fullFontCompiled`](#event-fullfontcompiled) | `CustomEvent` | 1 | 0 |
| [`glyphChanged`](#event-glyphchanged) | `CustomEvent` | 19 | 2 |
| [`glyphStackChanged`](#event-glyphstackchanged) | `CustomEvent` | 2 | 4 |
| [`layerFingerprintChanged`](#event-layerfingerprintchanged) | `CustomEvent` | 2 | 1 |
| [`layerLinkageChanged`](#event-layerlinkagechanged) | `CustomEvent` | 1 | 0 |
| [`outlineGuidelinesVisibilityChanged`](#event-outlineguidelinesvisibilitychanged) | `CustomEvent` | 1 | 1 |
| [`overviewInitialRenderComplete`](#event-overviewinitialrendercomplete) | `CustomEvent` | 1 | 0 |
| [`pluginFolderClosed`](#event-pluginfolderclosed) | `CustomEvent` | 1 | 1 |
| [`pluginTitleBarRedraw`](#event-plugintitlebarredraw) | `CustomEvent` | 1 | 1 |
| [`pwaLaunchFilesPending`](#event-pwalaunchfilespending) | `CustomEvent` | 1 | 1 |
| [`scriptEditorFileChanged`](#event-scripteditorfilechanged) | `CustomEvent` | 3 | 0 |
| [`unsupportedBrowserContinue`](#event-unsupportedbrowsercontinue) | `CustomEvent` | 1 | 1 |
| [`variationLocationChanged`](#event-variationlocationchanged) | `CustomEvent` | 3 | 1 |
| [`viewResized`](#event-viewresized) | `CustomEvent` | 4 | 1 |

<a id="event-canvasinitialready"></a>

## `canvasInitialReady`

- Description: Indicates that canvas initial is ready. Emitted on `window` from `setFont`. Detail keys: `openSessionId`, `source`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `source`
- Emit sites:
  - webapp/js/glyph-canvas.ts:1955 via method `setFont` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:4098 via module `top-level module code` on `window`

<a id="event-diskfileschanged"></a>

## `diskFilesChanged`

- Description: Dispatch event that file-browser listens for
- Emitted on: `window`
- Detail keys: `records`
- Emit sites:
  - webapp/js/filesystem-plugins.ts:312 via method `setupFileSystemObserver` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2858 via module `top-level module code` on `window`

<a id="event-diskfolderaccesschanged"></a>

## `diskFolderAccessChanged`

- Description: Indicates that disk folder access changed. Emitted on `window` from `selectDiskFolder`. Detail keys: `hasDiskAccess`, `source`.
- Emitted on: `window`
- Detail keys: `hasDiskAccess`, `source`
- Emit sites:
  - webapp/js/file-browser.ts:1381 via function `selectDiskFolder` on `window`
  - webapp/js/file-browser.ts:2847 via module `top-level module code` on `window`
- Listener sites:
  - webapp/js/overview-view.ts:465 via module `top-level module code` on `window`

<a id="event-editingfontcompiled"></a>

## `editingFontCompiled`

- Description: Compile the editing font for display in canvas.
- Emitted on: `window`
- Detail keys: `compilationMode`, `dragActive`, `duration`, `fontBytes`, `fontRevisionKey`
- Emit sites:
  - webapp/js/font-manager.ts:1793 via method `compileEditingFont` on `window`
- Listener sites:
  - webapp/js/change-bridge-init.ts:756 via function `waitForEditingFontCompileRevision` on `window`
  - webapp/js/font-manager.ts:2171 via method `ensureFullEditingCompile` on `window`
  - webapp/js/font-manager.ts:4107 via module `top-level module code` on `window`
  - webapp/js/glyph-canvas.ts:7056 via function `setupFontLoadingListener` on `window`

<a id="event-editormodechanged"></a>

## `editorModeChanged`

- Description: Dispatch event for URL sync (in editing mode, cursor = glyph index)
- Emitted on: `window`
- Detail keys: `mode`
- Emit sites:
  - webapp/js/glyph-canvas/textrun.ts:405 via method `selectGlyphByIndex` on `window`
  - webapp/js/glyph-canvas.ts:1267 via method `setupTextEditorEventHandlers` on `window`
  - webapp/js/glyph-canvas.ts:2050 via method `exitGlyphEditMode` on `window`
- Listener sites:
  - webapp/js/editor-plugins-ui.ts:123 via method `init` on `window`
  - webapp/js/editor-stack-preview-menu.ts:164 via function `initEditorStackPreviewMenu` on `window`
  - webapp/js/glyph-canvas.ts:6769 via function `initCanvas` on `window`
  - webapp/js/glyph-overview.ts:178 via method `init` on `window`
  - webapp/js/history-view.ts:130 via method `bindWindowEvents` on `window`
  - webapp/js/state-sync.ts:128 via function `initStateSync` on `window`
  - webapp/js/state-sync.ts:291 via function `initStateSync` on `window`

<a id="event-featurehistorycontextchanged"></a>

## `featureHistoryContextChanged`

- Description: Indicates that feature history context changed. Emitted on `window` from `notifyHistoryScopeChange`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/font-info.ts:1557 via method `notifyHistoryScopeChange` on `window`
- Listener sites:
  - webapp/js/history-view.ts:134 via method `bindWindowEvents` on `window`

<a id="event-filebrowserready"></a>

## `fileBrowserReady`

- Description: Indicates that file browser is ready. Emitted on `window` from `markFileBrowserReady`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/file-browser.ts:182 via function `markFileBrowserReady` on `window`
- Listener sites: none detected

<a id="event-fontloaded"></a>

## `fontLoaded`

- Description: Dispatch fontLoaded event to font manager
- Emitted on: `window`
- Detail keys: `babelfontJson`, `directoryHandle`, `fileHandle`, `path`, `sourcePlugin`
- Emit sites:
  - webapp/js/file-browser.ts:1158 via function `openFont` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:3910 via module `top-level module code` on `window`

<a id="event-fontmodelready"></a>

## `fontModelReady`

- Description: Notify ChangeBridge that the font model is ready
- Emitted on: `window`
- Detail keys: `babelfontData`, `path`
- Emit sites:
  - webapp/js/font-manager.ts:1100 via method `loadFont` on `window`
- Listener sites:
  - webapp/js/change-bridge-init.ts:1308 via module `top-level module code` on `window`
  - webapp/js/history-view.ts:116 via method `bindWindowEvents` on `window`

<a id="event-fontmodelsync"></a>

## `fontModelSync`

- Description: Custom app event for font model sync. Emitted on `window` from `initializeBridge`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/change-bridge-init.ts:1229 via function `initializeBridge` on `window`
- Listener sites:
  - webapp/js/font-info.ts:140 via method `init` on `window`

<a id="event-fontopenlifecycle"></a>

## `fontOpenLifecycle`

- Description: Custom app event for font open lifecycle. Emitted on `window` from `emitOpenLifecycle`. Detail keys: `openSessionId`, `phase`, `timestamp`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `phase`, `timestamp`
- Emit sites:
  - webapp/js/font-manager.ts:3897 via function `emitOpenLifecycle` on `window`
- Listener sites: none detected

<a id="event-fontready"></a>

## `fontReady`

- Description: Indicates that font is ready. Emitted on `window` from `reloadCurrentFontFromSource`. Detail keys: `openSessionId`, `openedAt`, `path`, `reloaded`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `openedAt`, `path`, `reloaded`
- Emit sites:
  - webapp/js/font-manager.ts:1036 via method `reloadCurrentFontFromSource` on `window`
  - webapp/js/font-manager.ts:3938 via function `dispatchFontReadyIfNeeded` on `window`
- Listener sites:
  - webapp/js/automation-runtime.ts:316 via module `top-level module code` on `window`
  - webapp/js/automation-runtime.ts:354 via function `waitForFontReady` on `window`
  - webapp/js/file-browser.ts:996 via function `waitForUrlOpenFontReady` on `window`
  - webapp/js/file-browser.ts:2955 via module `top-level module code` on `window`
  - webapp/js/file-browser.ts:2966 via module `top-level module code` on `window`
  - webapp/js/font-info.ts:139 via method `init` on `window`
  - webapp/js/glyph-canvas.ts:6825 via module `top-level module code` on `window`
  - webapp/js/overview-view.ts:450 via module `top-level module code` on `window`

<a id="event-fontspectorupdated"></a>

## `fontspectorUpdated`

- Description: Custom app event for fontspector updated. Emitted on `window` from `dispatchQcUpdate`. Detail keys: `availableProfiles`, `changeVersion`, `checks`, `error`, `profile`, `status`, `summary`.
- Emitted on: `window`
- Detail keys: `availableProfiles`, `changeVersion`, `checks`, `error`, `profile`, `status`, `summary`
- Emit sites:
  - webapp/js/full-font-compile-manager.ts:107 via function `dispatchQcUpdate` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:6761 via function `initCanvas` on `window`

<a id="event-fullfontcompiled"></a>

## `fullFontCompiled`

- Description: Indicates that full font compiled. Emitted on `window` from `runCompilationLoop`. Detail keys: `changeVersion`, `duration`, `fontBytes`, `qcChecks`, `qcSummary`.
- Emitted on: `window`
- Detail keys: `changeVersion`, `duration`, `fontBytes`, `qcChecks`, `qcSummary`
- Emit sites:
  - webapp/js/full-font-compile-manager.ts:303 via function `runCompilationLoop` on `window`
- Listener sites: none detected

<a id="event-glyphchanged"></a>

## `glyphChanged`

- Description: Refresh receiver-side Rust/cache state for a remote edit and request editing compilation only after the refresh has been queued.
- Emitted on: `window`
- Detail keys: `glyphName`, `glyphNames`, `layerId`
- Emit sites:
  - webapp/js/change-bridge-init.ts:689 via function `refreshGlyphOverviewAfterUndoRedo` on `window`
  - webapp/js/change-bridge-init.ts:973 via function `handleRemoteChangeRefresh` on `window`
  - webapp/js/font-manager.ts:3513 via method `saveLayerData` on `window`
  - webapp/js/font-manager.ts:3632 via function `run` on `window`
  - webapp/js/font-manager.ts:3790 via method `refreshGlyphsAfterModelBatch` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:7050 via method `refreshAfterStructuralLayerEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:11492 via method `onMouseUp` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:12582 via method `commitHoveredAddPointPreview` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:13253 via method `applyPathMutationAcrossLinkedLayers` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14059 via method `_finalizePathCutOrSplitEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14281 via method `completeOpenPathEndpointConnection` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14333 via method `completeOpenPathEndpointConnection` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14407 via method `closeOpenPathByMerge` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14448 via method `closeOpenPathByMerge` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14587 via method `finalizePendingCommandPathEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14600 via method `finalizePendingCommandPathEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:15821 via method `deleteSelectedNodes` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:15850 via method `deleteSelectedNodes` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:15863 via method `deleteSelectedNodes` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:169 via method `init` on `window`
  - webapp/js/history-view.ts:126 via method `bindWindowEvents` on `window`

<a id="event-glyphstackchanged"></a>

## `glyphStackChanged`

- Description: Build glyph_stack string from current state
- Emitted on: `window`
- Detail keys: `glyphStack`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:7667 via method `buildGlyphStack` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:7713 via method `rebuildGlyphStackWithNewLayer` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:6765 via function `initCanvas` on `window`
  - webapp/js/glyph-overview.ts:172 via method `init` on `window`
  - webapp/js/history-view.ts:122 via method `bindWindowEvents` on `window`
  - webapp/js/state-sync.ts:146 via function `initStateSync` on `window`

<a id="event-layerfingerprintchanged"></a>

## `layerFingerprintChanged`

- Description: Structural outline edits can change layer compatibility fingerprints.
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`
- Emit sites:
  - webapp/js/babelfont-model.ts:2513 via function `dispatchLayerFingerprintChanged` on `window`
  - webapp/js/change-bridge.ts:467 via method `_emitLayerFingerprintChangedEvents` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:397 via method `setupEventListeners` on `window`

<a id="event-layerlinkagechanged"></a>

## `layerLinkageChanged`

- Description: Indicates that layer linkage changed. Emitted on `window` from `refreshLayerLinkageUi`. Detail keys: `glyphName`, `layerId`, `linked`.
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`, `linked`
- Emit sites:
  - webapp/js/babelfont-model.ts:2329 via function `refreshLayerLinkageUi` on `window`
- Listener sites: none detected

<a id="event-outlineguidelinesvisibilitychanged"></a>

## `outlineGuidelinesVisibilityChanged`

- Description: Indicates that outline guidelines visibility changed. Emitted on `window` from `setGuidelinesVisible`. Detail keys: `visible`.
- Emitted on: `window`
- Detail keys: `visible`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:3284 via method `setGuidelinesVisible` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:169 via function `initEditorStackPreviewMenu` on `window`

<a id="event-overviewinitialrendercomplete"></a>

## `overviewInitialRenderComplete`

- Description: Indicates that overview initial render completed. Emitted on `window` from `renderOverviewAndEmit`. Detail keys: `glyphCount`, `openSessionId`, `reason`, `renderDurationMs`, `totalElapsedMs`.
- Emitted on: `window`
- Detail keys: `glyphCount`, `openSessionId`, `reason`, `renderDurationMs`, `totalElapsedMs`
- Emit sites:
  - webapp/js/overview-view.ts:227 via function `renderOverviewAndEmit` on `window`
- Listener sites: none detected

<a id="event-pluginfolderclosed"></a>

## `pluginFolderClosed`

- Description: Trigger UI update via event
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/filesystem-plugins.ts:410 via method `getTitleBarMenuItems` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2833 via module `top-level module code` on `window`

<a id="event-plugintitlebarredraw"></a>

## `pluginTitleBarRedraw`

- Description: Dispatch custom event that file-browser will listen for
- Emitted on: `window`
- Detail keys: `pluginId`
- Emit sites:
  - webapp/js/filesystem-plugins.ts:147 via method `redrawTitleBarButtons` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2971 via module `top-level module code` on `window`

<a id="event-pwalaunchfilespending"></a>

## `pwaLaunchFilesPending`

- Description: Indicates pending pwa launch files. Emitted on `window` from `registerPwaLaunchFileConsumer`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/bootstrap.ts:48 via function `registerPwaLaunchFileConsumer` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2804 via module `top-level module code` on `window`

<a id="event-scripteditorfilechanged"></a>

## `scriptEditorFileChanged`

- Description: Dispatch event for file change
- Emitted on: `window`
- Detail keys: `filePath`, `pluginId`
- Emit sites:
  - webapp/js/script-editor.ts:784 via function `handleNew` on `window`
  - webapp/js/script-editor.ts:905 via function `handleSave` on `window`
  - webapp/js/script-editor.ts:1084 via function `openFile` on `window`
- Listener sites: none detected

<a id="event-unsupportedbrowsercontinue"></a>

## `unsupportedBrowserContinue`

- Description: Custom app event for unsupported browser continue. Emitted on `window` from `showUnsupportedBrowserOverlay`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/bootstrap.ts:258 via function `showUnsupportedBrowserOverlay` on `window`
- Listener sites:
  - webapp/js/fonteditor.ts:208 via function `hideLoadingOverlayWhenAllowed` on `window`

<a id="event-variationlocationchanged"></a>

## `variationLocationChanged`

- Description: Apply state from StateManager to actual managers
- Emitted on: `window`
- Detail keys: `location`
- Emit sites:
  - webapp/js/glyph-canvas.ts:3208 via method `animateToLocation` on `window`
  - webapp/js/state-restore.ts:192 via function `applyStateToManagers` on `window`
  - webapp/js/state-sync.ts:175 via function `syncVariationLocation` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:188 via method `init` on `window`

<a id="event-viewresized"></a>

## `viewResized`

- Description: Notify view title buttons to update
- Emitted on: `window`
- Detail keys: `viewId`
- Emit sites:
  - webapp/js/keyboard-navigation.ts:647 via function `resizeView` on `window`
  - webapp/js/keyboard-navigation.ts:658 via function `resizeView` on `window`
  - webapp/js/keyboard-navigation.ts:824 via function `collapseActiveView` on `window`
  - webapp/js/keyboard-navigation.ts:838 via function `collapseActiveView` on `window`
- Listener sites:
  - webapp/js/view-title-buttons.ts:333 via function `setupStateObserver` on `window`

