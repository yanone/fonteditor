# JavaScript Event Reference

> Generated by `node scripts/generate-js-event-docs.mjs`. Do not edit this file by hand.

Generated: 2026-08-29T14:11:54.570Z

This document lists app-emitted JavaScript events discovered programmatically from source, along with generated descriptions, payload hints, emit sites, and listener sites.

## Summary

| Event | Class | Emitters | Listeners |
| --- | --- | ---: | ---: |
| [`canvasInitialReady`](#event-canvasinitialready) | `CustomEvent` | 1 | 1 |
| [`cloudAssetLocalizedToMemory`](#event-cloudassetlocalizedtomemory) | `CustomEvent` | 1 | 1 |
| [`cloudAssetRoleChanged`](#event-cloudassetrolechanged) | `CustomEvent` | 1 | 1 |
| [`cloudConnectionStatusChanged`](#event-cloudconnectionstatuschanged) | `CustomEvent` | 3 | 2 |
| [`counterpunch:glyph-data-ready`](#event-counterpunch-glyph-data-ready) | `Event` | 1 | 0 |
| [`counterpunch:qa-corpus-ready`](#event-counterpunch-qa-corpus-ready) | `Event` | 1 | 0 |
| [`counterpunch:update-available`](#event-counterpunch-update-available) | `CustomEvent` | 1 | 1 |
| [`diskFilesChanged`](#event-diskfileschanged) | `CustomEvent` | 1 | 1 |
| [`editingFontCompiled`](#event-editingfontcompiled) | `CustomEvent` | 1 | 5 |
| [`editorEditToolFlash`](#event-editoredittoolflash) | `CustomEvent` | 2 | 1 |
| [`editorEditToolsChanged`](#event-editoredittoolschanged) | `CustomEvent` | 1 | 1 |
| [`editorModeChanged`](#event-editormodechanged) | `CustomEvent` | 4 | 9 |
| [`editorPreviewAreaChanged`](#event-editorpreviewareachanged) | `CustomEvent` | 1 | 2 |
| [`featureHistoryContextChanged`](#event-featurehistorycontextchanged) | `CustomEvent` | 1 | 1 |
| [`fileBrowserReady`](#event-filebrowserready) | `CustomEvent` | 1 | 0 |
| [`filteredBooleanCompatibilityUpdated`](#event-filteredbooleancompatibilityupdated) | `CustomEvent` | 1 | 1 |
| [`folderPermissionsAutoPromptSettled`](#event-folderpermissionsautopromptsettled) | `CustomEvent` | 1 | 1 |
| [`folderPermissionsDialogClosed`](#event-folderpermissionsdialogclosed) | `CustomEvent` | 1 | 1 |
| [`folderSetupStatusChanged`](#event-foldersetupstatuschanged) | `CustomEvent` | 1 | 1 |
| [`fontEditorReady`](#event-fonteditorready) | `CustomEvent` | 1 | 1 |
| [`fontEditorReadyFailed`](#event-fonteditorreadyfailed) | `CustomEvent` | 1 | 1 |
| [`fontInteractiveReady`](#event-fontinteractiveready) | `CustomEvent` | 1 | 1 |
| [`fontLoaded`](#event-fontloaded) | `CustomEvent` | 3 | 3 |
| [`fontModelReady`](#event-fontmodelready) | `CustomEvent` | 1 | 5 |
| [`fontModelSync`](#event-fontmodelsync) | `CustomEvent` | 2 | 3 |
| [`fontOpenLifecycle`](#event-fontopenlifecycle) | `CustomEvent` | 1 | 1 |
| [`fontReady`](#event-fontready) | `CustomEvent` | 2 | 6 |
| [`fontSaveStateChanged`](#event-fontsavestatechanged) | `CustomEvent` | 1 | 0 |
| [`fontsFolderAccessChanged`](#event-fontsfolderaccesschanged) | `CustomEvent` | 4 | 1 |
| [`glyphCanvasRendered`](#event-glyphcanvasrendered) | `CustomEvent` | 1 | 3 |
| [`glyphCanvasViewportChanged`](#event-glyphcanvasviewportchanged) | `CustomEvent` | 1 | 1 |
| [`glyphChanged`](#event-glyphchanged) | `CustomEvent` | 12 | 2 |
| [`glyphStackChanged`](#event-glyphstackchanged) | `CustomEvent` | 5 | 4 |
| [`layerFingerprintChanged`](#event-layerfingerprintchanged) | `CustomEvent` | 3 | 1 |
| [`layerLinkageChanged`](#event-layerlinkagechanged) | `CustomEvent` | 1 | 0 |
| [`liveTextDiagnosticsRecorded`](#event-livetextdiagnosticsrecorded) | `CustomEvent` | 1 | 0 |
| [`managedFileChanged`](#event-managedfilechanged) | `CustomEvent` | 1 | 0 |
| [`nodeSnappingChanged`](#event-nodesnappingchanged) | `CustomEvent` | 1 | 1 |
| [`outlineBackgroundLayerModeChanged`](#event-outlinebackgroundlayermodechanged) | `CustomEvent` | 1 | 0 |
| [`outlineGuidelinesVisibilityChanged`](#event-outlineguidelinesvisibilitychanged) | `CustomEvent` | 1 | 1 |
| [`outlinePairedLayerVisibilityChanged`](#event-outlinepairedlayervisibilitychanged) | `CustomEvent` | 1 | 1 |
| [`overviewFollowStackScrollChanged`](#event-overviewfollowstackscrollchanged) | `CustomEvent` | 1 | 1 |
| [`overviewInitialRenderComplete`](#event-overviewinitialrendercomplete) | `CustomEvent` | 1 | 0 |
| [`pathFillOpacityChanged`](#event-pathfillopacitychanged) | `CustomEvent` | 1 | 0 |
| [`pluginFolderClosed`](#event-pluginfolderclosed) | `CustomEvent` | 1 | 2 |
| [`pluginTitleBarRedraw`](#event-plugintitlebarredraw) | `CustomEvent` | 1 | 1 |
| [`pwaLaunchFilesPending`](#event-pwalaunchfilespending) | `CustomEvent` | 1 | 1 |
| [`screenUnitScaleChanged`](#event-screenunitscalechanged) | `CustomEvent` | 1 | 1 |
| [`scriptEditorDocumentChanged`](#event-scripteditordocumentchanged) | `CustomEvent` | 1 | 1 |
| [`scriptEditorFileChanged`](#event-scripteditorfilechanged) | `CustomEvent` | 3 | 0 |
| [`settingsFolderAccessChanged`](#event-settingsfolderaccesschanged) | `CustomEvent` | 1 | 4 |
| [`showAllMetricsChanged`](#event-showallmetricschanged) | `CustomEvent` | 1 | 1 |
| [`unsupportedBrowserContinue`](#event-unsupportedbrowsercontinue) | `CustomEvent` | 1 | 1 |
| [`variationLocationChanged`](#event-variationlocationchanged) | `CustomEvent` | 3 | 1 |
| [`viewResized`](#event-viewresized) | `CustomEvent` | 2 | 1 |
| [`welcomeScreenSettled`](#event-welcomescreensettled) | `CustomEvent` | 1 | 1 |

<a id="event-canvasinitialready"></a>

## `canvasInitialReady`

- Description: Indicates that canvas initial is ready. Emitted on `window` from `setFont`. Detail keys: `openSessionId`, `source`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `source`
- Emit sites:
  - webapp/js/glyph-canvas.ts:4346 via method `setFont` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:6305 via module `top-level module code` on `window`

<a id="event-cloudassetlocalizedtomemory"></a>

## `cloudAssetLocalizedToMemory`

- Description: Custom app event for cloud asset localized to memory. Emitted on `window` from `handleDeletedAsset`. Detail keys: `assetId`, `message`, `path`.
- Emitted on: `window`
- Detail keys: `assetId`, `message`, `path`
- Emit sites:
  - webapp/js/cloud-plugin.ts:808 via method `handleDeletedAsset` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4550 via module `top-level module code` on `window`

<a id="event-cloudassetrolechanged"></a>

## `cloudAssetRoleChanged`

- Description: Indicates that cloud asset role changed. Emitted on `window` from `_cacheAssetRole`. Detail keys: `assetId`, `role`.
- Emitted on: `window`
- Detail keys: `assetId`, `role`
- Emit sites:
  - webapp/js/cloud-plugin.ts:857 via method `_cacheAssetRole` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:751 via module `top-level module code` on `window`

<a id="event-cloudconnectionstatuschanged"></a>

## `cloudConnectionStatusChanged`

- Description: Indicates that cloud connection status changed. Emitted on `window` from `_updateConnectionStatus`. Detail keys: `assetId`, `detail`, `pendingSyncCount`, `status`.
- Emitted on: `window`
- Detail keys: `assetId`, `detail`, `pendingSyncCount`, `status`
- Emit sites:
  - webapp/js/cloud-plugin.ts:1105 via method `_updateConnectionStatus` on `window`
  - webapp/js/cloud-plugin.ts:1132 via method `_updatePendingSyncCount` on `window`
  - webapp/js/cloud-plugin.ts:1338 via method `applyRelayedConnectionState` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:747 via module `top-level module code` on `window`
  - webapp/js/font-manager.ts:766 via method `init` on `window`

<a id="event-counterpunch-glyph-data-ready"></a>

## `counterpunch:glyph-data-ready`

- Description: Custom app event for counterpunch:glyph data ready. Emitted on `window` from `load`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/glyph-data.ts:167 via method `load` on `window`
- Listener sites: none detected

<a id="event-counterpunch-qa-corpus-ready"></a>

## `counterpunch:qa-corpus-ready`

- Description: Custom app event for counterpunch:qa corpus ready. Emitted on `window` from `load`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/auto-qa/auto-qa-corpus.ts:85 via method `load` on `window`
- Listener sites: none detected

<a id="event-counterpunch-update-available"></a>

## `counterpunch:update-available`

- Description: Custom app event for counterpunch:update available. Emitted on `document` from `setPendingUpdate`.
- Emitted on: `document`
- Detail expression(s): `pending`
- Emit sites:
  - webapp/js/update-manager.ts:180 via function `setPendingUpdate` on `document`
- Listener sites:
  - webapp/js/update-manager.ts:596 via function `initPreferencesVersionUi` on `document`

<a id="event-diskfileschanged"></a>

## `diskFilesChanged`

- Description: Dispatch event that file-browser listens for
- Emitted on: `window`
- Detail keys: `records`
- Emit sites:
  - webapp/js/filesystem-plugins.ts:418 via method `setupFileSystemObserver` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4430 via module `top-level module code` on `window`

<a id="event-editingfontcompiled"></a>

## `editingFontCompiled`

- Description: Compile the editing font for display in canvas.
- Emitted on: `window`
- Detail keys: `changeSource`, `compilationMode`, `dataFreshnessMode`, `dragActive`, `duration`, `editType`, `fontBytes`, `fontPath`, `fontRevisionKey`
- Emit sites:
  - webapp/js/font-manager.ts:3188 via method `compileEditingFont` on `window`
- Listener sites:
  - webapp/js/change-bridge-init.ts:1934 via function `waitForEditingFontCompileRevision` on `window`
  - webapp/js/compiled-edit-funnel.ts:81 via function `waitForEditingFontRevision` on `window`
  - webapp/js/font-manager.ts:3540 via method `ensureFullEditingCompile` on `window`
  - webapp/js/font-manager.ts:6314 via module `top-level module code` on `window`
  - webapp/js/glyph-canvas.ts:12893 via function `setupFontLoadingListener` on `window`

<a id="event-editoredittoolflash"></a>

## `editorEditToolFlash`

- Description: Custom app event for editor edit tool flash. Emitted on `window` from `requestEditToolFlash`. Detail keys: `toolId`.
- Emitted on: `window`
- Detail keys: `toolId`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:9556 via method `requestEditToolFlash` on `window`
  - webapp/js/tour-spotlight.ts:253 via function `flashRequiredTourTool` on `window`
- Listener sites:
  - webapp/js/editor-edit-tools-ui.ts:161 via function `initEditorEditToolsUi` on `window`

<a id="event-editoredittoolschanged"></a>

## `editorEditToolsChanged`

- Description: Indicates that editor edit tools changed. Emitted on `window` from `notifyEditToolsChanged`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:9552 via method `notifyEditToolsChanged` on `window`
- Listener sites:
  - webapp/js/editor-edit-tools-ui.ts:157 via function `initEditorEditToolsUi` on `window`

<a id="event-editormodechanged"></a>

## `editorModeChanged`

- Description: Dispatch event for URL sync (in editing mode, cursor = glyph index)
- Emitted on: `window`
- Detail keys: `mode`
- Emit sites:
  - webapp/js/glyph-canvas/textrun.ts:522 via method `selectGlyphByIndex` on `window`
  - webapp/js/glyph-canvas.ts:3344 via method `setupTextEditorEventHandlers` on `window`
  - webapp/js/glyph-canvas.ts:4440 via method `exitGlyphEditMode` on `window`
  - webapp/js/glyph-canvas.ts:4521 via method `resetForOpenedFontReplacement` on `window`
- Listener sites:
  - webapp/js/editor-edit-tools-ui.ts:153 via function `initEditorEditToolsUi` on `window`
  - webapp/js/editor-plugins-ui.ts:110 via method `init` on `window`
  - webapp/js/editor-stack-preview-menu.ts:307 via function `initEditorStackPreviewMenu` on `window`
  - webapp/js/glyph-overview.ts:381 via method `init` on `window`
  - webapp/js/history-view.ts:188 via method `bindWindowEvents` on `window`
  - webapp/js/state-sync.ts:205 via function `initStateSync` on `window`
  - webapp/js/state-sync.ts:375 via function `initStateSync` on `window`
  - webapp/js/tour-spotlight.ts:1028 via function `bindSlideInteraction` on `window`
  - webapp/js/tour-spotlight.ts:1454 via function `showTourSlide` on `window`

<a id="event-editorpreviewareachanged"></a>

## `editorPreviewAreaChanged`

- Description: Indicates that editor preview area changed. Emitted on `window` from `setPreviewArea`. Detail keys: `area`.
- Emitted on: `window`
- Detail keys: `area`
- Emit sites:
  - webapp/js/editor-preview-area-pref.ts:32 via function `setPreviewArea` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:334 via function `initEditorStackPreviewMenu` on `window`
  - webapp/js/glyph-canvas.ts:2067 via method `setupEventListeners` on `window`

<a id="event-featurehistorycontextchanged"></a>

## `featureHistoryContextChanged`

- Description: Indicates that feature history context changed. Emitted on `window` from `notifyHistoryScopeChange`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/font-info.ts:2377 via method `notifyHistoryScopeChange` on `window`
- Listener sites:
  - webapp/js/history-view.ts:192 via method `bindWindowEvents` on `window`

<a id="event-filebrowserready"></a>

## `fileBrowserReady`

- Description: Indicates that file browser is ready. Emitted on `window` from `markFileBrowserReady`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/file-browser.ts:732 via function `markFileBrowserReady` on `window`
- Listener sites: none detected

<a id="event-filteredbooleancompatibilityupdated"></a>

## `filteredBooleanCompatibilityUpdated`

- Description: Post-boolean VF fingerprints arrived; refresh compatibility UI.
- Emitted on: `window`
- Detail keys: `glyphNames`
- Emit sites:
  - webapp/js/babelfont-model.ts:11767 via method `scheduleFilteredBooleanFingerprintRefresh` on `window`
- Listener sites:
  - webapp/js/glyph-overview-filters.ts:253 via module `top-level module code` on `window`

<a id="event-folderpermissionsautopromptsettled"></a>

## `folderPermissionsAutoPromptSettled`

- Description: First-launch folder prompt finished (shown and closed, or skipped because setup is complete or File System Access is unavailable).
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/folder-permissions-dialog.ts:214 via function `settleFolderPermissionsAutoPrompt` on `window`
- Listener sites:
  - webapp/js/tour.ts:565 via function `bindTourListeners` on `window`

<a id="event-folderpermissionsdialogclosed"></a>

## `folderPermissionsDialogClosed`

- Description: Folder permissions dialog was closed (Continue, Skip, Escape, or overlay).
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/folder-permissions-dialog.ts:229 via function `closeDialog` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4402 via module `top-level module code` on `window`

<a id="event-foldersetupstatuschanged"></a>

## `folderSetupStatusChanged`

- Description: Folder project/settings access snapshot after a status refresh.
- Emitted on: `window`
- Detail expression(s): `cachedStatus`
- Emit sites:
  - webapp/js/folder-permissions-dialog.ts:182 via function `refreshFolderSetupStatus` on `window`
- Listener sites:
  - webapp/js/overview-view.ts:493 via module `top-level module code` on `window`

<a id="event-fonteditorready"></a>

## `fontEditorReady`

- Description: Indicates that font editor is ready. Emitted on `window` from `markFontEditorReady`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/editor-startup-ready.js:30 via function `markFontEditorReady` on `window`
- Listener sites:
  - webapp/js/editor-startup-ready.js:89 via function `waitForFontEditorReady` on `window`

<a id="event-fonteditorreadyfailed"></a>

## `fontEditorReadyFailed`

- Description: Custom app event for font editor ready failed. Emitted on `window` from `markFontEditorReadyFailed`. Detail keys: `error`.
- Emitted on: `window`
- Detail keys: `error`
- Emit sites:
  - webapp/js/editor-startup-ready.js:38 via function `markFontEditorReadyFailed` on `window`
- Listener sites:
  - webapp/js/editor-startup-ready.js:92 via function `waitForFontEditorReady` on `window`

<a id="event-fontinteractiveready"></a>

## `fontInteractiveReady`

- Description: Indicates that font interactive is ready. Emitted on `window` from `releaseStartupInteractivity`. Detail keys: `openSessionId`, `openedAt`, `path`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `openedAt`, `path`
- Emit sites:
  - webapp/js/font-manager.ts:6158 via function `releaseStartupInteractivity` on `window`
- Listener sites:
  - webapp/js/tour.ts:210 via function `waitForFontInteractiveReady` on `window`

<a id="event-fontloaded"></a>

## `fontLoaded`

- Description: After fontModelReady, the adapter's handler will rebind to the real bridge.
- Emitted on: `window`
- Detail keys: `babelfontJson`, `directoryHandle`, `fileHandle`, `path`, `sourcePlugin`
- Emit sites:
  - webapp/js/cloud-plugin.ts:2150 via method `_openAssetInternal` on `window`
  - webapp/js/file-browser.ts:1972 via function `openFont` on `window`
  - webapp/js/font-manager.ts:2222 via method `handleNewFont` on `window`
- Listener sites:
  - webapp/js/babelfont-model.ts:304 via module `top-level module code` on `window`
  - webapp/js/font-manager.ts:6092 via module `top-level module code` on `window`
  - webapp/js/glyph-overview-filters.ts:249 via module `top-level module code` on `window`

<a id="event-fontmodelready"></a>

## `fontModelReady`

- Description: Notify the patch sync engine bootstrap pipeline that the font model is ready
- Emitted on: `window`
- Detail keys: `babelfontData`, `path`
- Emit sites:
  - webapp/js/font-manager.ts:1986 via method `loadFont` on `window`
- Listener sites:
  - webapp/js/change-bridge-init.ts:3171 via module `top-level module code` on `window`
  - webapp/js/cloud-adapter.ts:767 via method `_subscribeFontModelReady` on `window`
  - webapp/js/cloud-plugin.ts:2145 via method `_openAssetInternal` on `window`
  - webapp/js/font-info.ts:849 via method `init` on `window`
  - webapp/js/history-view.ts:174 via method `bindWindowEvents` on `window`

<a id="event-fontmodelsync"></a>

## `fontModelSync`

- Description: Custom app event for font model sync. Emitted on `window` from `initializeBridge`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/change-bridge-init.ts:2988 via function `initializeBridge` on `window`
  - webapp/js/python-post-execution.ts:213 via function `commitPythonExecutionSyntheticChanges` on `window`
- Listener sites:
  - webapp/js/font-info.ts:850 via method `init` on `window`
  - webapp/js/glyph-canvas.ts:2066 via method `setupEventListeners` on `window`
  - webapp/js/kerning-editor-dialog.ts:140 via method `open` on `window`

<a id="event-fontopenlifecycle"></a>

## `fontOpenLifecycle`

- Description: Custom app event for font open lifecycle. Emitted on `window` from `emitOpenLifecycle`. Detail keys: `openSessionId`, `phase`, `timestamp`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `phase`, `timestamp`
- Emit sites:
  - webapp/js/font-manager.ts:6079 via function `emitOpenLifecycle` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:12531 via module `top-level module code` on `window`

<a id="event-fontready"></a>

## `fontReady`

- Description: Indicates that font is ready. Emitted on `window` from `reloadCurrentFontFromSource`. Detail keys: `openSessionId`, `openedAt`, `path`, `reloaded`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `openedAt`, `path`, `reloaded`
- Emit sites:
  - webapp/js/font-manager.ts:1918 via method `reloadCurrentFontFromSource` on `window`
  - webapp/js/font-manager.ts:6126 via function `dispatchFontReadyIfNeeded` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:1535 via function `queuePostOpenUiRefresh` on `window`
  - webapp/js/file-browser.ts:1762 via function `waitForUrlOpenFontReady` on `window`
  - webapp/js/file-browser.ts:4534 via module `top-level module code` on `window`
  - webapp/js/file-browser.ts:4546 via module `top-level module code` on `window`
  - webapp/js/glyph-canvas.ts:12535 via module `top-level module code` on `window`
  - webapp/js/overview-view.ts:474 via module `top-level module code` on `window`

<a id="event-fontsavestatechanged"></a>

## `fontSaveStateChanged`

- Description: Update button state based on current font
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/save-button.ts:181 via method `updateButtonState` on `window`
- Listener sites: none detected

<a id="event-fontsfolderaccesschanged"></a>

## `fontsFolderAccessChanged`

- Description: Refresh Disk UI after the Fonts folder handle is already selected.
- Emitted on: `window`
- Detail keys: `hasFontsFolderAccess`, `source`
- Emit sites:
  - webapp/js/file-browser.ts:2539 via function `applyFontsFolderSelection` on `window`
  - webapp/js/file-browser.ts:2703 via function `reEnableAccess` on `window`
  - webapp/js/file-browser.ts:4392 via module `top-level module code` on `window`
  - webapp/js/folder-permissions-dialog.ts:454 via function `handleFolderAction` on `window`
- Listener sites:
  - webapp/js/folder-permissions-dialog.ts:697 via function `bindStatusListeners` on `window`

<a id="event-glyphcanvasrendered"></a>

## `glyphCanvasRendered`

- Description: Custom app event for glyph canvas rendered. Emitted on `window` from `render`.
- Emitted on: `window`
- Detail expression(s): `nextRenderState`
- Emit sites:
  - webapp/js/glyph-canvas.ts:11697 via method `render` on `window`
- Listener sites:
  - webapp/js/tour-spotlight.ts:973 via function `bindSlideInteraction` on `window`
  - webapp/js/tour-spotlight.ts:1453 via function `showTourSlide` on `window`
  - webapp/js/tour.ts:307 via function `waitForTourSampleCompiledGlyphs` on `window`

<a id="event-glyphcanvasviewportchanged"></a>

## `glyphCanvasViewportChanged`

- Description: Canvas zoom (viewport.scale) changed.
- Emitted on: `window`
- Detail keys: `scale`
- Emit sites:
  - webapp/js/glyph-canvas/viewport.ts:94 via module `top-level module code` on `window`
- Listener sites:
  - webapp/js/glyph-canvas/text-layout-controls.ts:64 via method `bindGlobalListeners` on `window`

<a id="event-glyphchanged"></a>

## `glyphChanged`

- Description: Refresh glyph overview tiles from a seed glyph set so local live edits and committed local/remote change packets share the same invalidation logic for dependent composites and fallback rendering.
- Emitted on: `window`
- Detail keys: `glyphName`, `glyphNames`, `layerId`
- Emit sites:
  - webapp/js/change-bridge-init.ts:2347 via function `refreshGlyphOverviewFromGlyphNames` on `window`
  - webapp/js/font-manager.ts:5814 via method `refreshGlyphsAfterModelBatch` on `window`
  - webapp/js/font-manager.ts:5946 via method `stageLiveDragPreviewFromModel` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:13636 via method `onMouseUp` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14987 via method `commitHoveredAddPointPreview` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:16002 via method `decomposeContextMenuComponents` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:16111 via method `applyPathMutationAcrossLinkedLayers` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:16942 via method `_finalizePathCutOrSplitEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:17320 via method `completeOpenPathEndpointConnection` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:17415 via method `closeOpenPathByMerge` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:17519 via method `finalizePendingCommandPathEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:19003 via method `deleteSelectedNodes` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:372 via method `init` on `window`
  - webapp/js/history-view.ts:184 via method `bindWindowEvents` on `window`

<a id="event-glyphstackchanged"></a>

## `glyphStackChanged`

- Description: Build glyph_stack string from current state
- Emitted on: `window`
- Detail keys: `glyphStack`
- Emit sites:
  - webapp/js/delete-glyphs-ui-context.ts:82 via function `applyGlyphDeleteUiContext` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:1968 via method `replaceRootGlyphStackName` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:9371 via method `buildGlyphStack` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:9418 via method `rebuildGlyphStackWithNewLayer` on `window`
  - webapp/js/rename-glyphs-ui-context.ts:197 via function `applyGlyphRenameUiContext` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:375 via method `init` on `window`
  - webapp/js/history-view.ts:180 via method `bindWindowEvents` on `window`
  - webapp/js/state-sync.ts:228 via function `initStateSync` on `window`
  - webapp/js/tour-spotlight.ts:1002 via function `bindSlideInteraction` on `window`

<a id="event-layerfingerprintchanged"></a>

## `layerFingerprintChanged`

- Description: Structural outline edits can change layer compatibility fingerprints.
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`
- Emit sites:
  - webapp/js/babelfont-model.ts:2986 via function `dispatchLayerFingerprintChanged` on `window`
  - webapp/js/babelfont-model.ts:11782 via method `scheduleFilteredBooleanFingerprintRefresh` on `window`
  - webapp/js/patch-sync-engine.ts:747 via method `_emitLayerFingerprintChangedEvents` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:2062 via method `setupEventListeners` on `window`

<a id="event-layerlinkagechanged"></a>

## `layerLinkageChanged`

- Description: Indicates that layer linkage changed. Emitted on `window` from `refreshLayerLinkageUi`. Detail keys: `glyphName`, `layerId`, `linked`.
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`, `linked`
- Emit sites:
  - webapp/js/babelfont-model.ts:2802 via function `refreshLayerLinkageUi` on `window`
- Listener sites: none detected

<a id="event-livetextdiagnosticsrecorded"></a>

## `liveTextDiagnosticsRecorded`

- Description: Record a compact text-layout snapshot and caller trace when diagnostics are on.
- Emitted on: `window`
- Detail expression(s): `entry`
- Emit sites:
  - webapp/js/live-text-diagnostics.ts:107 via function `recordLiveTextDiagnostic` on `window`
- Listener sites: none detected

<a id="event-managedfilechanged"></a>

## `managedFileChanged`

- Description: Indicates that managed file changed. Emitted on `window` from `dispatchManagedFileChanged`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/managed-file-events.ts:207 via function `dispatchManagedFileChanged` on `window`
- Listener sites: none detected

<a id="event-nodesnappingchanged"></a>

## `nodeSnappingChanged`

- Description: Indicates that node snapping changed. Emitted on `window` from `setNodeSnappingEnabled`. Detail keys: `enabled`.
- Emitted on: `window`
- Detail keys: `enabled`
- Emit sites:
  - webapp/js/node-snapping-pref.ts:23 via function `setNodeSnappingEnabled` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:329 via function `initEditorStackPreviewMenu` on `window`

<a id="event-outlinebackgroundlayermodechanged"></a>

## `outlineBackgroundLayerModeChanged`

- Description: Indicates that outline background layer mode changed. Emitted on `window` from `toggleBackgroundLayerEditing`. Detail keys: `isEditingBackground`.
- Emitted on: `window`
- Detail keys: `isEditingBackground`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:2251 via method `toggleBackgroundLayerEditing` on `window`
- Listener sites: none detected

<a id="event-outlineguidelinesvisibilitychanged"></a>

## `outlineGuidelinesVisibilityChanged`

- Description: Indicates that outline guidelines visibility changed. Emitted on `window` from `setGuidelinesVisible`. Detail keys: `visible`.
- Emitted on: `window`
- Detail keys: `visible`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:1855 via method `setGuidelinesVisible` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:312 via function `initEditorStackPreviewMenu` on `window`

<a id="event-outlinepairedlayervisibilitychanged"></a>

## `outlinePairedLayerVisibilityChanged`

- Description: Indicates that outline paired layer visibility changed. Emitted on `window` from `setPairedLayerVisible`. Detail keys: `visible`.
- Emitted on: `window`
- Detail keys: `visible`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:2269 via method `setPairedLayerVisible` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:316 via function `initEditorStackPreviewMenu` on `window`

<a id="event-overviewfollowstackscrollchanged"></a>

## `overviewFollowStackScrollChanged`

- Description: Indicates that overview follow stack scroll changed. Emitted on `window` from `setOverviewFollowStackScrollEnabled`. Detail keys: `enabled`.
- Emitted on: `window`
- Detail keys: `enabled`
- Emit sites:
  - webapp/js/glyph-overview-follow-stack-pref.ts:17 via function `setOverviewFollowStackScrollEnabled` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:320 via function `initEditorStackPreviewMenu` on `window`

<a id="event-overviewinitialrendercomplete"></a>

## `overviewInitialRenderComplete`

- Description: Indicates that overview initial render completed. Emitted on `window` from `renderOverviewAndEmit`. Detail keys: `glyphCount`, `openSessionId`, `reason`, `renderDurationMs`, `totalElapsedMs`.
- Emitted on: `window`
- Detail keys: `glyphCount`, `openSessionId`, `reason`, `renderDurationMs`, `totalElapsedMs`
- Emit sites:
  - webapp/js/overview-view.ts:242 via function `renderOverviewAndEmit` on `window`
- Listener sites: none detected

<a id="event-pathfillopacitychanged"></a>

## `pathFillOpacityChanged`

- Description: Indicates that path fill opacity changed. Emitted on `window` from `setPathFillOpacity`. Detail keys: `value`.
- Emitted on: `window`
- Detail keys: `value`
- Emit sites:
  - webapp/js/path-fill-opacity-pref.ts:51 via function `setPathFillOpacity` on `window`
- Listener sites: none detected

<a id="event-pluginfolderclosed"></a>

## `pluginFolderClosed`

- Description: Trigger UI update via event
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/filesystem-plugins.ts:522 via method `getTitleBarMenuItems` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4375 via module `top-level module code` on `window`
  - webapp/js/folder-permissions-dialog.ts:700 via function `bindStatusListeners` on `window`

<a id="event-plugintitlebarredraw"></a>

## `pluginTitleBarRedraw`

- Description: Dispatch custom event that file-browser will listen for
- Emitted on: `window`
- Detail keys: `pluginId`
- Emit sites:
  - webapp/js/filesystem-plugins.ts:210 via method `redrawTitleBarButtons` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4562 via module `top-level module code` on `window`

<a id="event-pwalaunchfilespending"></a>

## `pwaLaunchFilesPending`

- Description: Indicates pending pwa launch files. Emitted on `window` from `registerPwaLaunchFileConsumer`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/bootstrap.ts:139 via function `registerPwaLaunchFileConsumer` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4346 via module `top-level module code` on `window`

<a id="event-screenunitscalechanged"></a>

## `screenUnitScaleChanged`

- Description: Screen calibration unitScale changed.
- Emitted on: `window`
- Detail keys: `unitScale`
- Emit sites:
  - webapp/js/glyph-canvas/point-size.ts:61 via function `setScreenUnitScale` on `window`
- Listener sites:
  - webapp/js/glyph-canvas/text-layout-controls.ts:67 via method `bindGlobalListeners` on `window`

<a id="event-scripteditordocumentchanged"></a>

## `scriptEditorDocumentChanged`

- Description: Indicates that script editor document changed. Emitted on `window` from `recordDocumentChange`.
- Emitted on: `window`
- Detail expression(s): `getDocumentState()`
- Emit sites:
  - webapp/js/script-editor.ts:107 via function `recordDocumentChange` on `window`
- Listener sites:
  - webapp/js/ai-assistant.ts:955 via method `initUI` on `window`

<a id="event-scripteditorfilechanged"></a>

## `scriptEditorFileChanged`

- Description: Dispatch event for file change
- Emitted on: `window`
- Detail keys: `filePath`, `pluginId`
- Emit sites:
  - webapp/js/script-editor.ts:1156 via function `handleNew` on `window`
  - webapp/js/script-editor.ts:1282 via function `handleSave` on `window`
  - webapp/js/script-editor.ts:1409 via function `openFile` on `window`
- Listener sites: none detected

<a id="event-settingsfolderaccesschanged"></a>

## `settingsFolderAccessChanged`

- Description: Indicates that settings folder access changed. Emitted on `window` from `dispatchAccessChanged`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/settings-folder.ts:369 via method `dispatchAccessChanged` on `window`
- Listener sites:
  - webapp/js/folder-permissions-dialog.ts:694 via function `bindStatusListeners` on `window`
  - webapp/js/overview-view.ts:489 via module `top-level module code` on `window`
  - webapp/js/run-python-script-dialog.ts:682 via function `ensureDom` on `window`
  - webapp/js/theme-switcher.ts:74 via method `init` on `window`

<a id="event-showallmetricschanged"></a>

## `showAllMetricsChanged`

- Description: Indicates that show all metrics changed. Emitted on `window` from `setShowAllMetricsEnabled`. Detail keys: `enabled`.
- Emitted on: `window`
- Detail keys: `enabled`
- Emit sites:
  - webapp/js/show-all-metrics-pref.ts:17 via function `setShowAllMetricsEnabled` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:324 via function `initEditorStackPreviewMenu` on `window`

<a id="event-unsupportedbrowsercontinue"></a>

## `unsupportedBrowserContinue`

- Description: Custom app event for unsupported browser continue. Emitted on `window` from `showUnsupportedBrowserOverlay`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/bootstrap.ts:344 via function `showUnsupportedBrowserOverlay` on `window`
- Listener sites:
  - webapp/js/fonteditor.ts:246 via function `hideLoadingOverlayWhenAllowed` on `window`

<a id="event-variationlocationchanged"></a>

## `variationLocationChanged`

- Description: Apply state from StateManager to actual managers
- Emitted on: `window`
- Detail keys: `location`
- Emit sites:
  - webapp/js/glyph-canvas.ts:6216 via method `animateToLocation` on `window`
  - webapp/js/state-restore.ts:255 via function `applyStateToManagers` on `window`
  - webapp/js/state-sync.ts:256 via function `syncVariationLocation` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:392 via method `init` on `window`

<a id="event-viewresized"></a>

## `viewResized`

- Description: Resize a view based on secondary shortcut behavior - Top row: small (activation min) → larger (~50%) → max - Bottom row 'expandToTarget': expand to resize target if smaller
- Emitted on: `window`
- Detail keys: `viewId`
- Emit sites:
  - webapp/js/keyboard-navigation.ts:696 via function `resizeView` on `window`
  - webapp/js/keyboard-navigation.ts:880 via function `collapseActiveView` on `window`
- Listener sites:
  - webapp/js/view-title-buttons.ts:311 via function `setupStateObserver` on `window`

<a id="event-welcomescreensettled"></a>

## `welcomeScreenSettled`

- Description: Fired after the welcome modal is dismissed, or skipped, once the loading overlay fade has finished.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/welcome-screen.ts:130 via function `dispatchWelcomeScreenSettled` on `window`
- Listener sites:
  - webapp/js/folder-permissions-dialog.ts:691 via function `bindStatusListeners` on `window`

