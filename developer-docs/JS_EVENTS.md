# JavaScript Event Reference

> Generated by `node generate-js-event-docs.mjs`. Do not edit this file by hand.

Generated: 2026-03-28T11:41:57.404Z

This document lists app-emitted JavaScript events discovered programmatically from source, along with generated descriptions, payload hints, emit sites, and listener sites.

## Summary

| Event | Class | Emitters | Listeners |
| --- | --- | ---: | ---: |
| [`canvasInitialReady`](#event-canvasinitialready) | `CustomEvent` | 1 | 1 |
| [`diskFilesChanged`](#event-diskfileschanged) | `CustomEvent` | 1 | 1 |
| [`diskFolderAccessChanged`](#event-diskfolderaccesschanged) | `CustomEvent` | 2 | 1 |
| [`editingFontCompiled`](#event-editingfontcompiled) | `CustomEvent` | 1 | 4 |
| [`editorModeChanged`](#event-editormodechanged) | `CustomEvent` | 3 | 7 |
| [`featureHistoryContextChanged`](#event-featurehistorycontextchanged) | `CustomEvent` | 1 | 1 |
| [`fileBrowserReady`](#event-filebrowserready) | `CustomEvent` | 1 | 0 |
| [`fontLoaded`](#event-fontloaded) | `CustomEvent` | 1 | 1 |
| [`fontModelReady`](#event-fontmodelready) | `CustomEvent` | 1 | 2 |
| [`fontModelSync`](#event-fontmodelsync) | `CustomEvent` | 1 | 1 |
| [`fontOpenEditingCompiled`](#event-fontopeneditingcompiled) | `CustomEvent` | 1 | 1 |
| [`fontOpenLifecycle`](#event-fontopenlifecycle) | `CustomEvent` | 1 | 1 |
| [`fontReady`](#event-fontready) | `CustomEvent` | 2 | 5 |
| [`fontspectorUpdated`](#event-fontspectorupdated) | `CustomEvent` | 1 | 1 |
| [`fullFontCompiled`](#event-fullfontcompiled) | `CustomEvent` | 1 | 0 |
| [`glyphChanged`](#event-glyphchanged) | `CustomEvent` | 11 | 2 |
| [`glyphStackChanged`](#event-glyphstackchanged) | `CustomEvent` | 2 | 4 |
| [`layerFingerprintChanged`](#event-layerfingerprintchanged) | `CustomEvent` | 1 | 1 |
| [`layerLinkageChanged`](#event-layerlinkagechanged) | `CustomEvent` | 1 | 0 |
| [`outlineGuidelinesVisibilityChanged`](#event-outlineguidelinesvisibilitychanged) | `CustomEvent` | 1 | 1 |
| [`overviewInitialRenderComplete`](#event-overviewinitialrendercomplete) | `CustomEvent` | 1 | 1 |
| [`pluginFolderClosed`](#event-pluginfolderclosed) | `CustomEvent` | 1 | 1 |
| [`pluginTitleBarRedraw`](#event-plugintitlebarredraw) | `CustomEvent` | 1 | 1 |
| [`pwaLaunchFilesPending`](#event-pwalaunchfilespending) | `CustomEvent` | 1 | 1 |
| [`scriptEditorFileChanged`](#event-scripteditorfilechanged) | `CustomEvent` | 3 | 0 |
| [`unsupportedBrowserContinue`](#event-unsupportedbrowsercontinue) | `CustomEvent` | 1 | 1 |
| [`variationLocationChanged`](#event-variationlocationchanged) | `CustomEvent` | 2 | 1 |
| [`viewResized`](#event-viewresized) | `CustomEvent` | 4 | 1 |

<a id="event-canvasinitialready"></a>

## `canvasInitialReady`

- Description: Indicates that canvas initial is ready. Emitted on `window` from `setFont`. Detail keys: `openSessionId`, `source`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `source`
- Emit sites:
  - webapp/js/glyph-canvas.ts:1673 via method `setFont` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:2667 via module `top-level module code` on `window`

<a id="event-diskfileschanged"></a>

## `diskFilesChanged`

- Description: Dispatch event that file-browser listens for
- Emitted on: `window`
- Detail keys: `records`
- Emit sites:
  - webapp/js/filesystem-plugins.ts:312 via method `setupFileSystemObserver` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2755 via module `top-level module code` on `window`

<a id="event-diskfolderaccesschanged"></a>

## `diskFolderAccessChanged`

- Description: Indicates that disk folder access changed. Emitted on `window` from `selectDiskFolder`. Detail keys: `hasDiskAccess`, `source`.
- Emitted on: `window`
- Detail keys: `hasDiskAccess`, `source`
- Emit sites:
  - webapp/js/file-browser.ts:1288 via function `selectDiskFolder` on `window`
  - webapp/js/file-browser.ts:2744 via module `top-level module code` on `window`
- Listener sites:
  - webapp/js/overview-view.ts:462 via module `top-level module code` on `window`

<a id="event-editingfontcompiled"></a>

## `editingFontCompiled`

- Description: Compile the editing font for display in canvas.
- Emitted on: `window`
- Detail keys: `compilationMode`, `dragActive`, `duration`, `fontBytes`, `fontRevisionKey`
- Emit sites:
  - webapp/js/font-manager.ts:1395 via method `compileEditingFont` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2902 via module `top-level module code` on `window`
  - webapp/js/font-manager.ts:1717 via method `ensureFullEditingCompile` on `window`
  - webapp/js/font-manager.ts:2700 via module `top-level module code` on `window`
  - webapp/js/glyph-canvas.ts:5498 via function `setupFontLoadingListener` on `window`

<a id="event-editormodechanged"></a>

## `editorModeChanged`

- Description: Dispatch event for URL sync (in editing mode, cursor = glyph index)
- Emitted on: `window`
- Detail keys: `mode`
- Emit sites:
  - webapp/js/glyph-canvas/textrun.ts:349 via method `selectGlyphByIndex` on `window`
  - webapp/js/glyph-canvas.ts:1101 via method `setupTextEditorEventHandlers` on `window`
  - webapp/js/glyph-canvas.ts:1768 via method `exitGlyphEditMode` on `window`
- Listener sites:
  - webapp/js/editor-plugins-ui.ts:123 via method `init` on `window`
  - webapp/js/editor-stack-preview-menu.ts:164 via function `initEditorStackPreviewMenu` on `window`
  - webapp/js/glyph-canvas.ts:5208 via function `initCanvas` on `window`
  - webapp/js/glyph-overview.ts:156 via method `init` on `window`
  - webapp/js/history-view.ts:129 via method `bindWindowEvents` on `window`
  - webapp/js/state-sync.ts:128 via function `initStateSync` on `window`
  - webapp/js/state-sync.ts:291 via function `initStateSync` on `window`

<a id="event-featurehistorycontextchanged"></a>

## `featureHistoryContextChanged`

- Description: Indicates that feature history context changed. Emitted on `window` from `notifyHistoryScopeChange`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/font-info.ts:1549 via method `notifyHistoryScopeChange` on `window`
- Listener sites:
  - webapp/js/history-view.ts:133 via method `bindWindowEvents` on `window`

<a id="event-filebrowserready"></a>

## `fileBrowserReady`

- Description: Indicates that file browser is ready. Emitted on `window` from `markFileBrowserReady`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/file-browser.ts:181 via function `markFileBrowserReady` on `window`
- Listener sites: none detected

<a id="event-fontloaded"></a>

## `fontLoaded`

- Description: Dispatch fontLoaded event to font manager
- Emitted on: `window`
- Detail keys: `babelfontJson`, `directoryHandle`, `fileHandle`, `path`, `sourcePlugin`
- Emit sites:
  - webapp/js/file-browser.ts:1065 via function `openFont` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:2489 via module `top-level module code` on `window`

<a id="event-fontmodelready"></a>

## `fontModelReady`

- Description: Notify ChangeBridge that the font model is ready
- Emitted on: `window`
- Detail keys: `babelfontData`, `path`
- Emit sites:
  - webapp/js/font-manager.ts:939 via method `loadFont` on `window`
- Listener sites:
  - webapp/js/change-bridge-init.ts:547 via module `top-level module code` on `window`
  - webapp/js/history-view.ts:115 via method `bindWindowEvents` on `window`

<a id="event-fontmodelsync"></a>

## `fontModelSync`

- Description: Custom app event for font model sync. Emitted on `window` from `initializeBridge`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/change-bridge-init.ts:466 via function `initializeBridge` on `window`
- Listener sites:
  - webapp/js/font-info.ts:137 via method `init` on `window`

<a id="event-fontopeneditingcompiled"></a>

## `fontOpenEditingCompiled`

- Description: Indicates that font open editing compiled. Emitted on `window` from `top-level module code`. Detail keys: `elapsedMs`, `openSessionId`, `openedAt`, `path`.
- Emitted on: `window`
- Detail keys: `elapsedMs`, `openSessionId`, `openedAt`, `path`
- Emit sites:
  - webapp/js/font-manager.ts:2707 via module `top-level module code` on `window`
- Listener sites:
  - webapp/js/overview-view.ts:435 via module `top-level module code` on `window`

<a id="event-fontopenlifecycle"></a>

## `fontOpenLifecycle`

- Description: Custom app event for font open lifecycle. Emitted on `window` from `emitOpenLifecycle`. Detail keys: `openSessionId`, `phase`, `timestamp`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `phase`, `timestamp`
- Emit sites:
  - webapp/js/font-manager.ts:2476 via function `emitOpenLifecycle` on `window`
- Listener sites:
  - webapp/js/overview-view.ts:393 via module `top-level module code` on `window`

<a id="event-fontready"></a>

## `fontReady`

- Description: Indicates that font is ready. Emitted on `window` from `reloadCurrentFontFromSource`. Detail keys: `openSessionId`, `openedAt`, `path`, `reloaded`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `openedAt`, `path`, `reloaded`
- Emit sites:
  - webapp/js/font-manager.ts:875 via method `reloadCurrentFontFromSource` on `window`
  - webapp/js/font-manager.ts:2518 via function `dispatchFontReadyIfNeeded` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2852 via module `top-level module code` on `window`
  - webapp/js/file-browser.ts:2864 via module `top-level module code` on `window`
  - webapp/js/font-info.ts:136 via method `init` on `window`
  - webapp/js/glyph-canvas.ts:5281 via module `top-level module code` on `window`
  - webapp/js/overview-view.ts:421 via module `top-level module code` on `window`

<a id="event-fontspectorupdated"></a>

## `fontspectorUpdated`

- Description: Custom app event for fontspector updated. Emitted on `window` from `dispatchQcUpdate`. Detail keys: `availableProfiles`, `changeVersion`, `checks`, `error`, `profile`, `status`, `summary`.
- Emitted on: `window`
- Detail keys: `availableProfiles`, `changeVersion`, `checks`, `error`, `profile`, `status`, `summary`
- Emit sites:
  - webapp/js/full-font-compile-manager.ts:98 via function `dispatchQcUpdate` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:5200 via function `initCanvas` on `window`

<a id="event-fullfontcompiled"></a>

## `fullFontCompiled`

- Description: Indicates that full font compiled. Emitted on `window` from `runCompilationLoop`. Detail keys: `changeVersion`, `duration`, `fontBytes`, `qcChecks`, `qcSummary`.
- Emitted on: `window`
- Detail keys: `changeVersion`, `duration`, `fontBytes`, `qcChecks`, `qcSummary`
- Emit sites:
  - webapp/js/full-font-compile-manager.ts:268 via function `runCompilationLoop` on `window`
- Listener sites: none detected

<a id="event-glyphchanged"></a>

## `glyphChanged`

- Description: Dispatch event for glyph overview to update tile
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`
- Emit sites:
  - webapp/js/change-bridge-init.ts:299 via function `refreshGlyphOverviewAfterUndoRedo` on `window`
  - webapp/js/font-manager.ts:2149 via method `saveLayerData` on `window`
  - webapp/js/font-manager.ts:2270 via function `run` on `window`
  - webapp/js/font-manager.ts:2383 via method `refreshGlyphsAfterModelBatch` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:4709 via method `onMouseUp` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:5652 via method `commitHoveredAddPointPreview` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:6272 via method `applyPathMutationAcrossLinkedLayers` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:6879 via method `openClosedPathAtNode` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:6974 via method `closeOpenPathByMerge` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:7057 via method `finalizePendingCommandPathEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:7998 via method `deleteSelectedNodes` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:147 via method `init` on `window`
  - webapp/js/history-view.ts:125 via method `bindWindowEvents` on `window`

<a id="event-glyphstackchanged"></a>

## `glyphStackChanged`

- Description: Build glyph_stack string from current state
- Emitted on: `window`
- Detail keys: `glyphStack`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:2759 via method `buildGlyphStack` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:2797 via method `rebuildGlyphStackWithNewLayer` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:5204 via function `initCanvas` on `window`
  - webapp/js/glyph-overview.ts:150 via method `init` on `window`
  - webapp/js/history-view.ts:121 via method `bindWindowEvents` on `window`
  - webapp/js/state-sync.ts:146 via function `initStateSync` on `window`

<a id="event-layerfingerprintchanged"></a>

## `layerFingerprintChanged`

- Description: Structural outline edits can change layer compatibility fingerprints.
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`
- Emit sites:
  - webapp/js/babelfont-model.ts:1996 via function `dispatchLayerFingerprintChanged` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:5217 via function `initCanvas` on `window`

<a id="event-layerlinkagechanged"></a>

## `layerLinkageChanged`

- Description: Indicates that layer linkage changed. Emitted on `window` from `refreshLayerLinkageUi`. Detail keys: `glyphName`, `layerId`, `linked`.
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`, `linked`
- Emit sites:
  - webapp/js/babelfont-model.ts:1812 via function `refreshLayerLinkageUi` on `window`
- Listener sites: none detected

<a id="event-outlineguidelinesvisibilitychanged"></a>

## `outlineGuidelinesVisibilityChanged`

- Description: Indicates that outline guidelines visibility changed. Emitted on `window` from `setGuidelinesVisible`. Detail keys: `visible`.
- Emitted on: `window`
- Detail keys: `visible`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:1194 via method `setGuidelinesVisible` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:169 via function `initEditorStackPreviewMenu` on `window`

<a id="event-overviewinitialrendercomplete"></a>

## `overviewInitialRenderComplete`

- Description: Indicates that overview initial render completed. Emitted on `window` from `renderOverviewAndEmit`. Detail keys: `glyphCount`, `openSessionId`, `reason`, `renderDurationMs`, `totalElapsedMs`.
- Emitted on: `window`
- Detail keys: `glyphCount`, `openSessionId`, `reason`, `renderDurationMs`, `totalElapsedMs`
- Emit sites:
  - webapp/js/overview-view.ts:193 via function `renderOverviewAndEmit` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:2688 via module `top-level module code` on `window`

<a id="event-pluginfolderclosed"></a>

## `pluginFolderClosed`

- Description: Trigger UI update via event
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/filesystem-plugins.ts:410 via method `getTitleBarMenuItems` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2730 via module `top-level module code` on `window`

<a id="event-plugintitlebarredraw"></a>

## `pluginTitleBarRedraw`

- Description: Dispatch custom event that file-browser will listen for
- Emitted on: `window`
- Detail keys: `pluginId`
- Emit sites:
  - webapp/js/filesystem-plugins.ts:147 via method `redrawTitleBarButtons` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2918 via module `top-level module code` on `window`

<a id="event-pwalaunchfilespending"></a>

## `pwaLaunchFilesPending`

- Description: Indicates pending pwa launch files. Emitted on `window` from `registerPwaLaunchFileConsumer`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/bootstrap.ts:49 via function `registerPwaLaunchFileConsumer` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:2701 via module `top-level module code` on `window`

<a id="event-scripteditorfilechanged"></a>

## `scriptEditorFileChanged`

- Description: Dispatch event for file change
- Emitted on: `window`
- Detail keys: `filePath`, `pluginId`
- Emit sites:
  - webapp/js/script-editor.ts:784 via function `handleNew` on `window`
  - webapp/js/script-editor.ts:905 via function `handleSave` on `window`
  - webapp/js/script-editor.ts:1084 via function `openFile` on `window`
- Listener sites: none detected

<a id="event-unsupportedbrowsercontinue"></a>

## `unsupportedBrowserContinue`

- Description: Custom app event for unsupported browser continue. Emitted on `window` from `showUnsupportedBrowserOverlay`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/bootstrap.ts:259 via function `showUnsupportedBrowserOverlay` on `window`
- Listener sites:
  - webapp/js/fonteditor.ts:208 via function `hideLoadingOverlayWhenAllowed` on `window`

<a id="event-variationlocationchanged"></a>

## `variationLocationChanged`

- Description: Notify glyph overview and other listeners about location change
- Emitted on: `window`
- Detail keys: `location`
- Emit sites:
  - webapp/js/glyph-canvas.ts:2646 via method `animateToLocation` on `window`
  - webapp/js/state-sync.ts:175 via function `syncVariationLocation` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:162 via method `init` on `window`

<a id="event-viewresized"></a>

## `viewResized`

- Description: Notify view title buttons to update
- Emitted on: `window`
- Detail keys: `viewId`
- Emit sites:
  - webapp/js/keyboard-navigation.ts:567 via function `resizeView` on `window`
  - webapp/js/keyboard-navigation.ts:578 via function `resizeView` on `window`
  - webapp/js/keyboard-navigation.ts:741 via function `collapseActiveView` on `window`
  - webapp/js/keyboard-navigation.ts:755 via function `collapseActiveView` on `window`
- Listener sites:
  - webapp/js/view-title-buttons.ts:309 via function `setupStateObserver` on `window`

