# JavaScript Event Reference

> Generated by `node generate-js-event-docs.mjs`. Do not edit this file by hand.

Generated: 2026-07-29T16:20:01.864Z

This document lists app-emitted JavaScript events discovered programmatically from source, along with generated descriptions, payload hints, emit sites, and listener sites.

## Summary

| Event | Class | Emitters | Listeners |
| --- | --- | ---: | ---: |
| [`canvasInitialReady`](#event-canvasinitialready) | `CustomEvent` | 2 | 1 |
| [`cloudAssetLocalizedToMemory`](#event-cloudassetlocalizedtomemory) | `CustomEvent` | 1 | 1 |
| [`cloudAssetRoleChanged`](#event-cloudassetrolechanged) | `CustomEvent` | 1 | 1 |
| [`cloudConnectionStatusChanged`](#event-cloudconnectionstatuschanged) | `CustomEvent` | 3 | 2 |
| [`counterpunch:glyph-data-ready`](#event-counterpunch-glyph-data-ready) | `Event` | 1 | 0 |
| [`diskFilesChanged`](#event-diskfileschanged) | `CustomEvent` | 1 | 1 |
| [`editingFontCompiled`](#event-editingfontcompiled) | `CustomEvent` | 1 | 5 |
| [`editorModeChanged`](#event-editormodechanged) | `CustomEvent` | 4 | 6 |
| [`featureHistoryContextChanged`](#event-featurehistorycontextchanged) | `CustomEvent` | 1 | 1 |
| [`fileBrowserReady`](#event-filebrowserready) | `CustomEvent` | 1 | 0 |
| [`fontEditorReady`](#event-fonteditorready) | `CustomEvent` | 1 | 1 |
| [`fontEditorReadyFailed`](#event-fonteditorreadyfailed) | `CustomEvent` | 1 | 1 |
| [`fontInteractiveReady`](#event-fontinteractiveready) | `CustomEvent` | 1 | 0 |
| [`fontLoaded`](#event-fontloaded) | `CustomEvent` | 3 | 1 |
| [`fontModelReady`](#event-fontmodelready) | `CustomEvent` | 1 | 6 |
| [`fontModelSync`](#event-fontmodelsync) | `CustomEvent` | 2 | 2 |
| [`fontOpenLifecycle`](#event-fontopenlifecycle) | `CustomEvent` | 1 | 1 |
| [`fontReady`](#event-fontready) | `CustomEvent` | 2 | 8 |
| [`fontSaveStateChanged`](#event-fontsavestatechanged) | `CustomEvent` | 1 | 0 |
| [`fontsFolderAccessChanged`](#event-fontsfolderaccesschanged) | `CustomEvent` | 2 | 0 |
| [`glyphCanvasRendered`](#event-glyphcanvasrendered) | `CustomEvent` | 1 | 0 |
| [`glyphChanged`](#event-glyphchanged) | `CustomEvent` | 11 | 2 |
| [`glyphStackChanged`](#event-glyphstackchanged) | `CustomEvent` | 5 | 3 |
| [`layerFingerprintChanged`](#event-layerfingerprintchanged) | `CustomEvent` | 2 | 1 |
| [`layerLinkageChanged`](#event-layerlinkagechanged) | `CustomEvent` | 1 | 0 |
| [`managedFileChanged`](#event-managedfilechanged) | `CustomEvent` | 1 | 0 |
| [`outlineBackgroundLayerModeChanged`](#event-outlinebackgroundlayermodechanged) | `CustomEvent` | 1 | 0 |
| [`outlineGuidelinesVisibilityChanged`](#event-outlineguidelinesvisibilitychanged) | `CustomEvent` | 1 | 1 |
| [`outlinePairedLayerVisibilityChanged`](#event-outlinepairedlayervisibilitychanged) | `CustomEvent` | 1 | 1 |
| [`overviewInitialRenderComplete`](#event-overviewinitialrendercomplete) | `CustomEvent` | 1 | 0 |
| [`pluginFolderClosed`](#event-pluginfolderclosed) | `CustomEvent` | 1 | 1 |
| [`pluginTitleBarRedraw`](#event-plugintitlebarredraw) | `CustomEvent` | 1 | 1 |
| [`pwaLaunchFilesPending`](#event-pwalaunchfilespending) | `CustomEvent` | 1 | 1 |
| [`scriptEditorDocumentChanged`](#event-scripteditordocumentchanged) | `CustomEvent` | 1 | 1 |
| [`scriptEditorFileChanged`](#event-scripteditorfilechanged) | `CustomEvent` | 3 | 0 |
| [`settingsFolderAccessChanged`](#event-settingsfolderaccesschanged) | `CustomEvent` | 1 | 3 |
| [`unsupportedBrowserContinue`](#event-unsupportedbrowsercontinue) | `CustomEvent` | 1 | 1 |
| [`variationLocationChanged`](#event-variationlocationchanged) | `CustomEvent` | 3 | 1 |
| [`viewResized`](#event-viewresized) | `CustomEvent` | 4 | 1 |

<a id="event-canvasinitialready"></a>

## `canvasInitialReady`

- Description: Indicates that canvas initial is ready. Emitted on `window` from `setFont`. Detail keys: `source`.
- Emitted on: `window`
- Detail keys: `source`
- Emit sites:
  - webapp/js/glyph-canvas.ts:2401 via method `setFont` on `window`
  - webapp/js/glyph-canvas.ts:2410 via method `setFont` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:6481 via module `top-level module code` on `window`

<a id="event-cloudassetlocalizedtomemory"></a>

## `cloudAssetLocalizedToMemory`

- Description: Custom app event for cloud asset localized to memory. Emitted on `window` from `handleDeletedAsset`. Detail keys: `assetId`, `message`, `path`.
- Emitted on: `window`
- Detail keys: `assetId`, `message`, `path`
- Emit sites:
  - webapp/js/cloud-plugin.ts:798 via method `handleDeletedAsset` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4248 via module `top-level module code` on `window`

<a id="event-cloudassetrolechanged"></a>

## `cloudAssetRoleChanged`

- Description: Indicates that cloud asset role changed. Emitted on `window` from `_cacheAssetRole`. Detail keys: `assetId`, `role`.
- Emitted on: `window`
- Detail keys: `assetId`, `role`
- Emit sites:
  - webapp/js/cloud-plugin.ts:847 via method `_cacheAssetRole` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:667 via module `top-level module code` on `window`

<a id="event-cloudconnectionstatuschanged"></a>

## `cloudConnectionStatusChanged`

- Description: Indicates that cloud connection status changed. Emitted on `window` from `_updateConnectionStatus`. Detail keys: `assetId`, `detail`, `pendingSyncCount`, `status`.
- Emitted on: `window`
- Detail keys: `assetId`, `detail`, `pendingSyncCount`, `status`
- Emit sites:
  - webapp/js/cloud-plugin.ts:1095 via method `_updateConnectionStatus` on `window`
  - webapp/js/cloud-plugin.ts:1122 via method `_updatePendingSyncCount` on `window`
  - webapp/js/cloud-plugin.ts:1328 via method `applyRelayedConnectionState` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:663 via module `top-level module code` on `window`
  - webapp/js/font-manager.ts:682 via method `init` on `window`

<a id="event-counterpunch-glyph-data-ready"></a>

## `counterpunch:glyph-data-ready`

- Description: Custom app event for counterpunch:glyph data ready. Emitted on `window` from `load`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/glyph-data.ts:167 via method `load` on `window`
- Listener sites: none detected

<a id="event-diskfileschanged"></a>

## `diskFilesChanged`

- Description: Dispatch event that file-browser listens for
- Emitted on: `window`
- Detail keys: `records`
- Emit sites:
  - webapp/js/filesystem-plugins.ts:418 via method `setupFileSystemObserver` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4128 via module `top-level module code` on `window`

<a id="event-editingfontcompiled"></a>

## `editingFontCompiled`

- Description: Compile the editing font for display in canvas.
- Emitted on: `window`
- Detail keys: `changeSource`, `compilationMode`, `dataFreshnessMode`, `dragActive`, `duration`, `editType`, `fontBytes`, `fontPath`, `fontRevisionKey`
- Emit sites:
  - webapp/js/font-manager.ts:2938 via method `compileEditingFont` on `window`
- Listener sites:
  - webapp/js/change-bridge-init.ts:1870 via function `waitForEditingFontCompileRevision` on `window`
  - webapp/js/compiled-edit-funnel.ts:81 via function `waitForEditingFontRevision` on `window`
  - webapp/js/font-manager.ts:3324 via method `ensureFullEditingCompile` on `window`
  - webapp/js/font-manager.ts:6490 via module `top-level module code` on `window`
  - webapp/js/glyph-canvas.ts:9399 via function `setupFontLoadingListener` on `window`

<a id="event-editormodechanged"></a>

## `editorModeChanged`

- Description: Dispatch event for URL sync (in editing mode, cursor = glyph index)
- Emitted on: `window`
- Detail keys: `mode`
- Emit sites:
  - webapp/js/glyph-canvas/textrun.ts:418 via method `selectGlyphByIndex` on `window`
  - webapp/js/glyph-canvas.ts:1698 via method `setupTextEditorEventHandlers` on `window`
  - webapp/js/glyph-canvas.ts:2504 via method `exitGlyphEditMode` on `window`
  - webapp/js/glyph-canvas.ts:2583 via method `resetForOpenedFontReplacement` on `window`
- Listener sites:
  - webapp/js/editor-plugins-ui.ts:110 via method `init` on `window`
  - webapp/js/editor-stack-preview-menu.ts:175 via function `initEditorStackPreviewMenu` on `window`
  - webapp/js/glyph-overview.ts:192 via method `init` on `window`
  - webapp/js/history-view.ts:113 via method `bindWindowEvents` on `window`
  - webapp/js/state-sync.ts:137 via function `initStateSync` on `window`
  - webapp/js/state-sync.ts:302 via function `initStateSync` on `window`

<a id="event-featurehistorycontextchanged"></a>

## `featureHistoryContextChanged`

- Description: Indicates that feature history context changed. Emitted on `window` from `notifyHistoryScopeChange`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/font-info.ts:2372 via method `notifyHistoryScopeChange` on `window`
- Listener sites:
  - webapp/js/history-view.ts:117 via method `bindWindowEvents` on `window`

<a id="event-filebrowserready"></a>

## `fileBrowserReady`

- Description: Indicates that file browser is ready. Emitted on `window` from `markFileBrowserReady`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/file-browser.ts:722 via function `markFileBrowserReady` on `window`
- Listener sites: none detected

<a id="event-fonteditorready"></a>

## `fontEditorReady`

- Description: Indicates that font editor is ready. Emitted on `window` from `markFontEditorReady`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/editor-startup-ready.js:30 via function `markFontEditorReady` on `window`
- Listener sites:
  - webapp/js/editor-startup-ready.js:89 via function `waitForFontEditorReady` on `window`

<a id="event-fonteditorreadyfailed"></a>

## `fontEditorReadyFailed`

- Description: Custom app event for font editor ready failed. Emitted on `window` from `markFontEditorReadyFailed`. Detail keys: `error`.
- Emitted on: `window`
- Detail keys: `error`
- Emit sites:
  - webapp/js/editor-startup-ready.js:38 via function `markFontEditorReadyFailed` on `window`
- Listener sites:
  - webapp/js/editor-startup-ready.js:92 via function `waitForFontEditorReady` on `window`

<a id="event-fontinteractiveready"></a>

## `fontInteractiveReady`

- Description: Indicates that font interactive is ready. Emitted on `window` from `releaseStartupInteractivity`. Detail keys: `openSessionId`, `openedAt`, `path`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `openedAt`, `path`
- Emit sites:
  - webapp/js/font-manager.ts:6341 via function `releaseStartupInteractivity` on `window`
- Listener sites: none detected

<a id="event-fontloaded"></a>

## `fontLoaded`

- Description: After fontModelReady, the adapter's handler will rebind to the real bridge.
- Emitted on: `window`
- Detail keys: `babelfontJson`, `directoryHandle`, `fileHandle`, `path`, `sourcePlugin`
- Emit sites:
  - webapp/js/cloud-plugin.ts:2140 via method `_openAssetInternal` on `window`
  - webapp/js/file-browser.ts:1905 via function `openFont` on `window`
  - webapp/js/font-manager.ts:2049 via method `handleNewFont` on `window`
- Listener sites:
  - webapp/js/font-manager.ts:6275 via module `top-level module code` on `window`

<a id="event-fontmodelready"></a>

## `fontModelReady`

- Description: Notify the patch sync engine bootstrap pipeline that the font model is ready
- Emitted on: `window`
- Detail keys: `babelfontData`, `path`
- Emit sites:
  - webapp/js/font-manager.ts:1869 via method `loadFont` on `window`
- Listener sites:
  - webapp/js/change-bridge-init.ts:2986 via module `top-level module code` on `window`
  - webapp/js/cloud-adapter.ts:759 via method `_subscribeFontModelReady` on `window`
  - webapp/js/cloud-plugin.ts:2135 via method `_openAssetInternal` on `window`
  - webapp/js/font-info.ts:844 via method `init` on `window`
  - webapp/js/history-view.ts:99 via method `bindWindowEvents` on `window`
  - webapp/js/overview-view.ts:464 via module `top-level module code` on `window`

<a id="event-fontmodelsync"></a>

## `fontModelSync`

- Description: Custom app event for font model sync. Emitted on `window` from `initializeBridge`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/change-bridge-init.ts:2801 via function `initializeBridge` on `window`
  - webapp/js/python-post-execution.ts:206 via function `commitPythonExecutionSyntheticChanges` on `window`
- Listener sites:
  - webapp/js/font-info.ts:845 via method `init` on `window`
  - webapp/js/glyph-canvas.ts:766 via method `setupEventListeners` on `window`

<a id="event-fontopenlifecycle"></a>

## `fontOpenLifecycle`

- Description: Custom app event for font open lifecycle. Emitted on `window` from `emitOpenLifecycle`. Detail keys: `openSessionId`, `phase`, `timestamp`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `phase`, `timestamp`
- Emit sites:
  - webapp/js/font-manager.ts:6262 via function `emitOpenLifecycle` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:9126 via module `top-level module code` on `window`

<a id="event-fontready"></a>

## `fontReady`

- Description: Indicates that font is ready. Emitted on `window` from `reloadCurrentFontFromSource`. Detail keys: `openSessionId`, `openedAt`, `path`, `reloaded`.
- Emitted on: `window`
- Detail keys: `openSessionId`, `openedAt`, `path`, `reloaded`
- Emit sites:
  - webapp/js/font-manager.ts:1804 via method `reloadCurrentFontFromSource` on `window`
  - webapp/js/font-manager.ts:6309 via function `dispatchFontReadyIfNeeded` on `window`
- Listener sites:
  - webapp/js/automation-runtime.ts:316 via module `top-level module code` on `window`
  - webapp/js/automation-runtime.ts:354 via function `waitForFontReady` on `window`
  - webapp/js/file-browser.ts:1525 via function `queuePostOpenUiRefresh` on `window`
  - webapp/js/file-browser.ts:1695 via function `waitForUrlOpenFontReady` on `window`
  - webapp/js/file-browser.ts:4232 via module `top-level module code` on `window`
  - webapp/js/file-browser.ts:4244 via module `top-level module code` on `window`
  - webapp/js/glyph-canvas.ts:9130 via module `top-level module code` on `window`
  - webapp/js/overview-view.ts:468 via module `top-level module code` on `window`

<a id="event-fontsavestatechanged"></a>

## `fontSaveStateChanged`

- Description: Update button state based on current font
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/save-button.ts:176 via method `updateButtonState` on `window`
- Listener sites: none detected

<a id="event-fontsfolderaccesschanged"></a>

## `fontsFolderAccessChanged`

- Description: Change the selected Fonts folder (Disk plugin root) while preserving the current Disk-context refresh, observer setup, and dependent-view notifications.
- Emitted on: `window`
- Detail keys: `hasFontsFolderAccess`, `source`
- Emit sites:
  - webapp/js/file-browser.ts:2413 via function `changeFontsFolder` on `window`
  - webapp/js/file-browser.ts:4117 via module `top-level module code` on `window`
- Listener sites: none detected

<a id="event-glyphcanvasrendered"></a>

## `glyphCanvasRendered`

- Description: Custom app event for glyph canvas rendered. Emitted on `window` from `render`.
- Emitted on: `window`
- Detail expression(s): `nextRenderState`
- Emit sites:
  - webapp/js/glyph-canvas.ts:8684 via method `render` on `window`
- Listener sites: none detected

<a id="event-glyphchanged"></a>

## `glyphChanged`

- Description: Refresh glyph overview tiles from a seed glyph set so local live edits and committed local/remote change packets share the same invalidation logic for dependent composites and fallback rendering.
- Emitted on: `window`
- Detail keys: `glyphName`, `glyphNames`, `layerId`
- Emit sites:
  - webapp/js/change-bridge-init.ts:2268 via function `refreshGlyphOverviewFromGlyphNames` on `window`
  - webapp/js/font-manager.ts:6000 via method `refreshGlyphsAfterModelBatch` on `window`
  - webapp/js/font-manager.ts:6132 via method `stageLiveDragPreviewFromModel` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:13801 via method `onMouseUp` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:14850 via method `commitHoveredAddPointPreview` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:15569 via method `applyPathMutationAcrossLinkedLayers` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:16402 via method `_finalizePathCutOrSplitEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:16739 via method `completeOpenPathEndpointConnection` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:16831 via method `closeOpenPathByMerge` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:16943 via method `finalizePendingCommandPathEdit` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:18291 via method `deleteSelectedNodes` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:183 via method `init` on `window`
  - webapp/js/history-view.ts:109 via method `bindWindowEvents` on `window`

<a id="event-glyphstackchanged"></a>

## `glyphStackChanged`

- Description: Build glyph_stack string from current state
- Emitted on: `window`
- Detail keys: `glyphStack`
- Emit sites:
  - webapp/js/delete-glyphs-ui-context.ts:82 via function `applyGlyphDeleteUiContext` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:3539 via method `replaceRootGlyphStackName` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:9714 via method `buildGlyphStack` on `window`
  - webapp/js/glyph-canvas/outline-editor.ts:9760 via method `rebuildGlyphStackWithNewLayer` on `window`
  - webapp/js/rename-glyphs-ui-context.ts:197 via function `applyGlyphRenameUiContext` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:186 via method `init` on `window`
  - webapp/js/history-view.ts:105 via method `bindWindowEvents` on `window`
  - webapp/js/state-sync.ts:155 via function `initStateSync` on `window`

<a id="event-layerfingerprintchanged"></a>

## `layerFingerprintChanged`

- Description: Structural outline edits can change layer compatibility fingerprints.
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`
- Emit sites:
  - webapp/js/babelfont-model.ts:2730 via function `dispatchLayerFingerprintChanged` on `window`
  - webapp/js/patch-sync-engine.ts:722 via method `_emitLayerFingerprintChangedEvents` on `window`
- Listener sites:
  - webapp/js/glyph-canvas.ts:762 via method `setupEventListeners` on `window`

<a id="event-layerlinkagechanged"></a>

## `layerLinkageChanged`

- Description: Indicates that layer linkage changed. Emitted on `window` from `refreshLayerLinkageUi`. Detail keys: `glyphName`, `layerId`, `linked`.
- Emitted on: `window`
- Detail keys: `glyphName`, `layerId`, `linked`
- Emit sites:
  - webapp/js/babelfont-model.ts:2546 via function `refreshLayerLinkageUi` on `window`
- Listener sites: none detected

<a id="event-managedfilechanged"></a>

## `managedFileChanged`

- Description: Indicates that managed file changed. Emitted on `window` from `dispatchManagedFileChanged`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/managed-file-events.ts:207 via function `dispatchManagedFileChanged` on `window`
- Listener sites: none detected

<a id="event-outlinebackgroundlayermodechanged"></a>

## `outlineBackgroundLayerModeChanged`

- Description: Indicates that outline background layer mode changed. Emitted on `window` from `toggleBackgroundLayerEditing`. Detail keys: `isEditingBackground`.
- Emitted on: `window`
- Detail keys: `isEditingBackground`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:3753 via method `toggleBackgroundLayerEditing` on `window`
- Listener sites: none detected

<a id="event-outlineguidelinesvisibilitychanged"></a>

## `outlineGuidelinesVisibilityChanged`

- Description: Indicates that outline guidelines visibility changed. Emitted on `window` from `setGuidelinesVisible`. Detail keys: `visible`.
- Emitted on: `window`
- Detail keys: `visible`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:3426 via method `setGuidelinesVisible` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:180 via function `initEditorStackPreviewMenu` on `window`

<a id="event-outlinepairedlayervisibilitychanged"></a>

## `outlinePairedLayerVisibilityChanged`

- Description: Indicates that outline paired layer visibility changed. Emitted on `window` from `setPairedLayerVisible`. Detail keys: `visible`.
- Emitted on: `window`
- Detail keys: `visible`
- Emit sites:
  - webapp/js/glyph-canvas/outline-editor.ts:3771 via method `setPairedLayerVisible` on `window`
- Listener sites:
  - webapp/js/editor-stack-preview-menu.ts:184 via function `initEditorStackPreviewMenu` on `window`

<a id="event-overviewinitialrendercomplete"></a>

## `overviewInitialRenderComplete`

- Description: Indicates that overview initial render completed. Emitted on `window` from `renderOverviewAndEmit`. Detail keys: `glyphCount`, `openSessionId`, `reason`, `renderDurationMs`, `totalElapsedMs`.
- Emitted on: `window`
- Detail keys: `glyphCount`, `openSessionId`, `reason`, `renderDurationMs`, `totalElapsedMs`
- Emit sites:
  - webapp/js/overview-view.ts:227 via function `renderOverviewAndEmit` on `window`
- Listener sites: none detected

<a id="event-pluginfolderclosed"></a>

## `pluginFolderClosed`

- Description: Trigger UI update via event
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/filesystem-plugins.ts:522 via method `getTitleBarMenuItems` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4101 via module `top-level module code` on `window`

<a id="event-plugintitlebarredraw"></a>

## `pluginTitleBarRedraw`

- Description: Dispatch custom event that file-browser will listen for
- Emitted on: `window`
- Detail keys: `pluginId`
- Emit sites:
  - webapp/js/filesystem-plugins.ts:210 via method `redrawTitleBarButtons` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4260 via module `top-level module code` on `window`

<a id="event-pwalaunchfilespending"></a>

## `pwaLaunchFilesPending`

- Description: Indicates pending pwa launch files. Emitted on `window` from `registerPwaLaunchFileConsumer`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/bootstrap.ts:139 via function `registerPwaLaunchFileConsumer` on `window`
- Listener sites:
  - webapp/js/file-browser.ts:4072 via module `top-level module code` on `window`

<a id="event-scripteditordocumentchanged"></a>

## `scriptEditorDocumentChanged`

- Description: Indicates that script editor document changed. Emitted on `window` from `recordDocumentChange`.
- Emitted on: `window`
- Detail expression(s): `getDocumentState()`
- Emit sites:
  - webapp/js/script-editor.ts:97 via function `recordDocumentChange` on `window`
- Listener sites:
  - webapp/js/ai-assistant.ts:883 via method `initUI` on `window`

<a id="event-scripteditorfilechanged"></a>

## `scriptEditorFileChanged`

- Description: Dispatch event for file change
- Emitted on: `window`
- Detail keys: `filePath`, `pluginId`
- Emit sites:
  - webapp/js/script-editor.ts:1103 via function `handleNew` on `window`
  - webapp/js/script-editor.ts:1232 via function `handleSave` on `window`
  - webapp/js/script-editor.ts:1359 via function `openFile` on `window`
- Listener sites: none detected

<a id="event-settingsfolderaccesschanged"></a>

## `settingsFolderAccessChanged`

- Description: Indicates that settings folder access changed. Emitted on `window` from `dispatchAccessChanged`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/settings-folder.ts:341 via method `dispatchAccessChanged` on `window`
- Listener sites:
  - webapp/js/overview-view.ts:483 via module `top-level module code` on `window`
  - webapp/js/run-python-script-dialog.ts:683 via function `ensureDom` on `window`
  - webapp/js/theme-switcher.ts:82 via method `init` on `window`

<a id="event-unsupportedbrowsercontinue"></a>

## `unsupportedBrowserContinue`

- Description: Custom app event for unsupported browser continue. Emitted on `window` from `showUnsupportedBrowserOverlay`.
- Emitted on: `window`
- Detail keys: none detected
- Emit sites:
  - webapp/js/bootstrap.ts:344 via function `showUnsupportedBrowserOverlay` on `window`
- Listener sites:
  - webapp/js/fonteditor.ts:225 via function `hideLoadingOverlayWhenAllowed` on `window`

<a id="event-variationlocationchanged"></a>

## `variationLocationChanged`

- Description: Apply state from StateManager to actual managers
- Emitted on: `window`
- Detail keys: `location`
- Emit sites:
  - webapp/js/glyph-canvas.ts:4280 via method `animateToLocation` on `window`
  - webapp/js/state-restore.ts:188 via function `applyStateToManagers` on `window`
  - webapp/js/state-sync.ts:183 via function `syncVariationLocation` on `window`
- Listener sites:
  - webapp/js/glyph-overview.ts:202 via method `init` on `window`

<a id="event-viewresized"></a>

## `viewResized`

- Description: Notify view title buttons to update
- Emitted on: `window`
- Detail keys: `viewId`
- Emit sites:
  - webapp/js/keyboard-navigation.ts:548 via function `resizeView` on `window`
  - webapp/js/keyboard-navigation.ts:559 via function `resizeView` on `window`
  - webapp/js/keyboard-navigation.ts:725 via function `collapseActiveView` on `window`
  - webapp/js/keyboard-navigation.ts:739 via function `collapseActiveView` on `window`
- Listener sites:
  - webapp/js/view-title-buttons.ts:331 via function `setupStateObserver` on `window`

