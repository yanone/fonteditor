# Glyph Editor Basics

The Editor view is where visual type design decisions are made point by point. For beginners, the challenge is not only learning tools but learning a repeatable rhythm: inspect, adjust, compare, and validate. This page focuses on that rhythm so early edits are controlled and confidence grows quickly.

## Summary

The Editor view is where you draw and refine outlines with direct visual feedback. Starting with small, reversible changes helps new users understand contour behavior before moving to advanced workflows such as interpolation across multiple masters.

## Core Actions for Beginners

The editor offers several fundamental operations. You can select points and contours to modify them, move points and handles with precision, and adjust your zoom level to balance detailed work with overall rhythm assessment. Hold `Tab` to immediately inspect sidebearings and other distances without leaving the canvas. In text mode this takes over the normal tab character, which keeps the shortcut immediate and predictable. Saving after each successful change helps build confidence and provides natural checkpoints.

## A Safe First Practice

When beginning, consider opening a simple glyph such as **H** or **O**, then moving a single point slightly to observe the effect. Check how this change affects side bearings and visual balance, and experiment with undo and redo to build familiarity with the recovery process.

## Suggested Screenshots

### Screenshot 1 — Editor canvas with selected contour point

- Filename: `editor-01-01-point-selection.png`
- Capture: one glyph with selected node and handles visible.
- Suggested annotations:
    1. Glyph canvas
    2. Selected point
    3. Handle direction
- Alt text: Glyph editor showing a selected node and Bézier handles.

### Screenshot 2 — Before/after tiny outline adjustment

- Filename: `editor-01-02-before-after-adjustment.png`
- Capture: split image (before vs after) of a small contour edit.
- Suggested annotations:
    1. Original contour
    2. Modified contour
- Alt text: Comparison of a glyph outline before and after a small edit.

## Related Pages

- [Axes, Masters, and Interpolation](02-axes-masters-interpolation.md)
- [Keyboard Shortcuts](../reference/keyboard-shortcuts.md)
